name: Core Edu - Web Part

on:
  push:
    branches: [ main ]
    paths:
      - 'web/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repo
      uses: actions/checkout@v3

    - name: Login to GHCR
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Build Web
      uses: docker/build-push-action@v5
      with:
        context: ./web
        push: true
        tags: ghcr.io/sverdlovsky/core-edu-web:latest
        cache-from: type=registry,ref=ghcr.io/sverdlovsky/core-edu-web:latest
        cache-to: type=registry,ref=ghcr.io/sverdlovsky/core-edu-web:latest,mode=max
        platforms: linux/amd64

